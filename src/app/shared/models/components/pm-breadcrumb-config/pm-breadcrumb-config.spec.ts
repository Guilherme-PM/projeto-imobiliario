import { PmBreadcrumbConfig } from './pm-breadcrumb-config';

describe('PmBreadcrumbConfig', () => {
  it('should create an instance', () => {
    expect(new PmBreadcrumbConfig()).toBeTruthy();
  });
});
